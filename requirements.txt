# TrendWatcher Dependencies
# =========================
# Installeer met: pip install -r requirements.txt

# Flask web framework
Flask==3.0.0

# HTTP requests library voor API calls
requests==2.31.0

# WSGI server voor production deployment (optioneel)
gunicorn==21.2.0

# Voor development (wordt automatisch meegeleverd met Flask)
Werkzeug==3.0.1
